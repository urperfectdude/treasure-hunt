# Copy to .env for local dev. These are public (baked into the static
# build) by design — Supabase's anon key is meant to be used from the
# browser and is only as powerful as the RLS policies allow.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
